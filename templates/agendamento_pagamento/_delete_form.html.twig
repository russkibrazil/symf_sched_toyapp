<form method="post" action="{{ path('agendamento_pagamento_delete', {'id': agendamento_pagamento.id}) }}" onsubmit="return confirm('Você tem certeza que deseja apagar esse item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agendamento_pagamento.id) }}">
    <button class="btn btn-danger">Apagar</button>
</form>
