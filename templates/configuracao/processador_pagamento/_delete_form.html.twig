<form method="post" action="{{ path('empresa_pagamento_processador_delete', {'cnpj': epp.empresa.cnpj, 'processador': epp.processador}) }}" onsubmit="return confirm('Você tem certeza que deseja apagar esse item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ epp.empresa.cnpj ~ epp.processador) }}">
    <button type="submit" class="btn btn-danger">Apagar</button>
</form>
